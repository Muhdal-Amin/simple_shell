#include "shell.h"


/**
 * line_tokenizer - processes the input text into commands and arguements
 * @lineptr : string containing input text
 *
 * Return: pointer to array of tokenized text
 */
char *line_tokenizer(char *lineptr)
{
